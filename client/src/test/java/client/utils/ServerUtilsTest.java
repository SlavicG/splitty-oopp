package client.utils;

class ServerUtilsTest {
    /*
    Configuration configuration = new Configuration();
    ServerUtils server = new ServerUtils(configuration);

    static User user = new User(1, "Abdullah", "@email.com", "504", "2323");
    static User user2 = new User(1, "Abdullah", "@email.com", "504", "5000");
    //User user3= server.addUsers(user);
    //User user4= server.addUsers(user2);
    @Test
    void constructorTest() {
        try {
            ServerUtils server1 = new ServerUtils(configuration);
        } catch (Exception e) {
            e.printStackTrace();
            fail("Test failed due to an unexpected error: " + e.getMessage());
        }

    }

    @Test
    void getAndAddUsers() {
        //List<User> users = server.getUsers();
        //assertTrue(users.contains(user));
        //assertEquals(user, server.getUserById(1));
        //server.updateUser(user2, 1);
        //assertEquals(user2, server.getUserById(1));
        //server.updateUser(user, 1);
        //List<User> users1 = new ArrayList<>();
        //users1.add(user);
        //users1.add(user2);
        //User user2 = server.addUsers(new User());
        //users1.add(user2);
        //User user3 = server.addUsers(new User());
        //users1.add(user3);
        //User user4 = server.addUsers(new User());
        //users1.add(user4);
        //User user5 = server.addUsers(new User());
        //users1.add(user5);
        //User user6 = server.addUsers(new User());
        //users1.add(user6);
        // List<User> usersAfter = server.getUsers();
        //assertEquals(usersAfter.size(),users1.size());
    }

    @Test
    void getAndAddEvents() {
        //User user3= server.addUsers(user);
        //User user2= server.addUsers(user);
        List<Integer> userIds = new ArrayList<>();
        userIds.add(1);
        userIds.add(2);
        List<Expense> expenses = new ArrayList<>();
        expenses.add(new Expense());
        Event event = new Event(1,"patat", userIds,expenses, null);
        //Event event1 = server.addEvent(event);
        //List<Event> events = server.getEvents();
        //assertTrue(events.contains(event1));
        Event event3 = new Event(1,"patat123", userIds, expenses, null);
        //server.updateEvent(event3,1);
        //Event event2 = server.getEventById(1);
        //assertEquals(event3.getTitle(),event2.getTitle());
        //server.updateEvent(event, 1);
        //server.deleteEvent(1);
    }

    @Test
    void getAndAddExpenses() {
        LocalDateTime now = LocalDateTime.now();
        List<Integer> users = new ArrayList<>();
        List<Expense> expenses = new ArrayList<>();
        Event event = new Event( 1,"patat", users, expenses, null);
        Expense expense = new Expense();
        expenses.add(expense);
        //server.addEvent(event);
        //List<Expense> expense1 = server.getExpenses(1);
        //assertTrue(expense1.equals(expenses));
    }

    @Test
    void getAndAddPersons() {
        Person person = new Person("Abdullah", "Alpsoy");
        //server.addPerson(person);
        //List<Person> person1 = server.getPersons();
        //assertTrue(person1.contains(person));
    }

    @Test
    void getAndAddDebt() {
        User user = new User(7, "Abdullah", "@email.com", "504", "2323");
        User user1 = new User(6, "Ilker", "@email.com", "5046", "2324");
        Debt debt = new Debt(5, user1, user, 50.0);
        //server.addDebt(debt);
        //List<Quote> quote1 = server.getQuotes();
        //assertTrue(quote1.contains(debt));
    }
    */
}
